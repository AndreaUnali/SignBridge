# Input Audio

Place your audio files in this folder before running the pipeline in **Audio mode**.

Supported formats: `.mp3`, `.wav`, `.flac`, `.ogg`, `.opus`, `.m4a`, `.aac`

Files placed here are ignored by git (see `.gitignore`) so your personal recordings are never committed.
